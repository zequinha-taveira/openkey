[package]
name = "openkey-config"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Gerenciamento de configuração persistente do firmware OpenKey (no_std)"

[dependencies]
openkey-platform = { workspace = true }

[features]
default = []
